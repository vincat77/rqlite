public static class VersionInfo
{
    // Version of rqlite.
    public const string Version = "8";

    // Commit this code was built at.
    public const string Commit = "unknown";

    // Branch the code was built from.
    public const string Branch = "unknown";

    // Buildtime is the timestamp when the build took place.
    public const string Buildtime = "unknown";

    // CompilerCommand is the compiler used to build the binary.
    public const string CompilerCommand = "unknown";
}
